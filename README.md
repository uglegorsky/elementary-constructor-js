# Elementary JS site constructor

## Build Setup

```bash
# install dependencies
$ npm install

# serve with hot reload at localhost:4200
$ npm run serve

# build for production and launch server
$ npm run build
```
